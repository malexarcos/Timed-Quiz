function generateQuiz(questions, quizContainers, resultsContainer, submitButton){
    function showQuestions(questions, quizContainers){
        //code....
    }
    function showResults(questions, quizContainers, resultsContainer){
        // code....
    }
    // enter the questions
    showQuestions(questions, quizContainers);

    // submit/show the results
    submitButton(questions, quizContainers, resultsContainer);
}

var questions = [
    {
        question: "what is java"
    }
]